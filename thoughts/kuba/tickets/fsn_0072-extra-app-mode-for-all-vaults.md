# Default app config containing all vaults

- Now we support app configs packages/web/src/lib/app-config.ts
- I want to have one more app config whitch should be default if no config param provided
- The app running in this mode should show all all vaults regardless app field from vault list
- Keep all other features like conditional components, theming, basing on tags etc.
- Theme should be attached to the vault page based on the tag or app value - waht's better?
- Navigation sidebar should contain all general purpose links, exclude protocol specific pages