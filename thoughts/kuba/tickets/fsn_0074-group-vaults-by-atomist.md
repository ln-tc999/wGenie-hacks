# Group vaults by atomists, create config for each one

- This is follow up to thoughts/kuba/tickets/fsn_0073-update-fusion-vaults-list.md
- Read vault configs in src/fusion/plasmaVaults/config
- Read all vault configs in $ARUMENTS, ask if not provided
- Identify atomists to group by
- Create separate app config for each atomist
- Copy atomists (curator) logo
- For now use default CSS theme for each atomist, I will fix that later
- Update package.json with script commands