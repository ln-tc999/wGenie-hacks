[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm install -g pnpm@10.28.2",
  "cd /app && pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "pnpm --filter @wgenie/fusion-sdk build"
]

[start]
cmd = "cd packages/ponder && pnpm start"
